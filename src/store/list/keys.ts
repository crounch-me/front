export enum ListActions {
  CREATE = 'CREATE',
  CREATED = 'CREATED',
  GETOWNERS = 'GETOWNERS',
};

export enum ListMutations {
  ADD = 'ADD',
  SET = 'SET',
  RESET = 'RESET',
};

export enum ListGetters {
  LISTS = 'LISTS',
};
